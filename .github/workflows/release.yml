name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    name: Release (Dagger)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build and export release artifacts
        uses: dagger/dagger-for-github@v8.4.1
        with:
          version: "0.20.8"
          module: .
          dagger-flags: "--progress plain"
          call: >-
            release
            --source=.
            --tag=${{ github.ref_name }}
            --commit=${{ github.sha }}
            --built-at=${{ github.event.head_commit.timestamp || github.run_started_at }}
            export
            --path=dist

      - name: Generate release notes from CHANGELOG
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          chmod +x scripts/release-notes.sh
          ./scripts/release-notes.sh extract "$VERSION" > release-notes.md
          echo "--- release-notes.md ---"
          cat release-notes.md

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false
          body_path: release-notes.md
          files: |
            dist/*.tar.gz
            dist/SHA256SUMS
            dist/install.sh
